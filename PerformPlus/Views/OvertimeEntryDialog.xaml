<Window x:Class="PerformPlus.Views.OvertimeEntryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:md="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf"
        Title="{DynamicResource AddOvertimeTitle}"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignDarkPaper}">

    <StackPanel Margin="20" Width="300">

        <DatePicker SelectedDate="{Binding Entry.EntryDate, Mode=TwoWay}"
                    md:HintAssist.Hint="{DynamicResource DateHint}"
                    Margin="0,0,0,10"/>

        <TextBox Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                 md:HintAssist.Hint="{DynamicResource HoursHint}"
                 Text="{Binding Entry.Hours, Mode=TwoWay}" 
                 Margin="0,0,0,20"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{DynamicResource SaveButton}"
                    Margin="0,0,10,0"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Click="Save_Click"
                    IsDefault="True"/>
            <Button Content="{DynamicResource CancelButton}"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Click="Cancel_Click"
                    IsCancel="True"/>
        </StackPanel>
    </StackPanel>
</Window>
